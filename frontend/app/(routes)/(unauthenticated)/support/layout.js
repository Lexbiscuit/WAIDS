export const metadata = {
  title: "Support",
  //   Description for Support Page
  description: "Support Page for the WAIDS Web Application.",
};

export default function SupportLayout({ children }) {
  return <>{children}</>;
}
